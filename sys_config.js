"use strict";

(function(){
	var system = "local",
		diffDirectory = "/diffs/",
		rawDirectory = "/raw/";

	module.exports = {
		system: system,
		diffDirectory: diffDirectory,
		rawDirectory: rawDirectory
	};
})();