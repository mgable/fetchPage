"use strict";

(function(){
	var system = "aws",
		diffDirectory = "/diffs/",
		rawDirectory = "/raw-test/";

	module.exports = {
		system: system,
		diffDirectory: diffDirectory,
		rawDirectory: rawDirectory
	};
})();